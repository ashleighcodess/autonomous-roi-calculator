{
  "name": "autonomous-roi-calculator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step needed'",
    "deploy": "wrangler pages deploy public",
    "dev": "wrangler pages dev public"
  },
  "devDependencies": {
    "wrangler": "^3.109.0"
  }
}
